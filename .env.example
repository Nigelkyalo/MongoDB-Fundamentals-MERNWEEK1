# .env.example
# Copy this file to `.env` (do NOT commit `.env`) and replace the placeholder values.
# Example MongoDB Atlas URI format (use your cluster host, user and password):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/plp_bookstore?retryWrites=true&w=majority

MONGODB_URI=
